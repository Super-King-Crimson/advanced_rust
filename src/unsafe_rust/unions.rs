pub fn explain() {
    println!("A union a struct, but only one field of it is used per instance of that union");

    //They basically only exist to interface with unions in C code
    println!("Check out the Rust Reference \
    (https://doc.rust-lang.org/reference/items/unions.html) \
    to learn more about unions");
}